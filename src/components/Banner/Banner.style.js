
import { css } from 'lit';


export const BannerStyle = css`
  .banner {
    background-color: light-dark(var(--purple), var(--blue));
    color: var(--white);
    padding: 2rem;
    margin: 0;
    display: flex;
    justify-content: center;
    font-size: 1.5rem;
  }
`;
